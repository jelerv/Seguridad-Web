
**docker-compose.yml** (básico para empezar)
```yaml
services:
  juice-shop:
    image: bkimminich/juice-shop
    ports:
      - "3000:3000"
  crapi:
    image: hapi/hapi-crapi:latest   # o usa la versión oficial si existe
    ports:
      - "8888:8888"
  vulnerable-flask:
    build: ./modules/03-broken-access-control/vulnerable
    ports:
      - "5000:5000"
